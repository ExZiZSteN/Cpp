# Шестаков Михаил ИТ-2 Вариант 5


## Задачи


### Задание 1. Задача 1
`
Точка координат.
Создайте сущность Точка, расположенную на двумерной плоскости, которая описывается:
    Координата Х: число
    Координата Y: число
    Может возвращать текстовое представление вида “{X;Y}”
Необходимо создать три точки с разными координатами и вывести на экран их текстовое
представление.
`

##### Пример 1:
Точка 1 {3,1}
Точка 2 {4.41,41}
Точка 3 {2,6.41}

Результат:
{3,1}
{4.41,41}
{2,6.41}


#### Описание решения
Инициализировал класс `Point` с двумя полями `x` и `y`, также создал метод `cordinates` класса, который выводит координаты точки. Точки храню в массиве размерностью 3. Через цикл `for` просим пользователя ввести значения координат для трех точек, после этого пробегаемся по массиву и выводим значения методом `cordinates`

### Задание 1. Задача 3
`
Имена.
Создайте сущность Имя, которая описывается тремя параметрами: Фамилия, Личное имя,
Отчество. Имя может быть приведено к строковому виду, включающему традиционное
представление всех трех параметров: Фамилия Имя Отчество (например “Иванов Иван
Иванович”). Необходимо предусмотреть возможность того, что какой-либо из параметров может
быть не задан, и в этом случае он не учитывается при приведении к текстовому виду.
Необходимо создать следующие имена:
    Клеопатра
    Пушкин Александр Сергеевич
    Маяковский Владимир
Обратите внимание, что при выводе на экран, не заданные параметры никак не участвуют в
образовании строки.
`

##### Пример 1:
Ivanov Ivan Ivanovich
Petrov Petr
Mikhail

Результат:
Ivanov Ivan Ivanovich
Kazimir Kazimirovich
Mikhail

#### Описание решения
Инициализировал класс `Name` с полями `lastName` `firstName` `middleName`, реализовал метод `printName` класса, так что он выводит имя на основе полученных данных. Инициализируем массив размерностью 3, где храним вводимые значения. Передаем значения в класс через конструктор и выводим имя.

### Задание 2. Задача 1
`
Прямая.
Создайте сущность Линия, расположенную на двумерной плоскости, которая описывается:
 Координата начала: Точка
 Координата конца: Точка
 Может возвращать текстовое представление вида “Линия от {X1;Y1} до {X2;Y2}”
Для указания координат нужно использовать сущность Точка, разработанную в задании 1.1. Создайте
линии со следующими характеристиками:
1. Линия 1 с началом в т. {1;3} и концом в т.{23;8}.
2. Линия 2, горизонтальная, на высоте 10, от точки 5 до точки 25.
3. Линия 3, которая начинается всегда там же, где начинается линия 1, и заканчивается всегда там
же, где заканчивается линия 2. Таким образом, если положение первой или второй линии
меняется, то меняется и третья линия.
4. После создания всех трех объектов измените координаты первой и второй линий, причем
сделайте это таким образом, чтобы положение третьей линии соответствовало требованиям
пункта 3.
5. Измените координаты первой линии так, чтобы при этом не изменились, координаты третьей
линии.
`

##### Пример 1:
Линия 1:
начало {1,3} - конец {23,8}
Линия 2:
начало {5,10} - конец {25,0}



#### Описание решения
Инициализировал класс `Line` и `Point`. Сначала инициализируем два объекта `Line`. Для изминения координат линии, переинициализируется объкты.


### Задание 3. Задача 3
`
Города.
Создайте сущность Город, которая будет представлять собой точку на карте со следующими
характеристиками:
 Название города
 Набор путей к следующим городам, где путь представляет собой сочетание Города и
стоимости поездки в него.
Кроме того, Город может возвращать текстовое представление, в виде названия города и списка
связанных с ним городов (в виде пары: “название:стоимость”).
Используя разработанную сущность реализуйте схему, представленную на рисунке 2
`

##### Пример 1:
на рисунке в лабе

#### Описание решения
Инициализировал класс `City`, который является графом
, у класса есть поля `name` и `routes`. Добавляем связи между вершинами с помощью метода `addRoute`, которые принмает на вход массив в которм хранятя пары (название города, стоимость поездки), считвается каждая пара и добавляется маршрут


### Задание 4. Задача 8
`
Города.
Создайте сущность Город, которая будет представлять собой точку на карте со следующими
характеристиками:
 Название города
 Набор путей к следующим городам, где путь представляет собой сочетание Города и
стоимости поездки в него.
Кроме того, Город может возвращать текстовое представление, в виде названия города и списка
связанных с ним городов (в виде пары: “название:стоимость”).
Используя разработанную сущность реализуйте схему, представленную на рисунке 2
`

##### Пример 1:
на рисунке в лабе

#### Описание решения
Инициализировал класс `City`, который является графом
, у класса есть поля `name` и `routes`. Добавляем связи между вершинами с помощью метода `addRoute`, которые принмает на вход массив в которм хранятя пары (название города, стоимость поездки), считвается каждая пара и добавляется маршрут

### Задание 5. Задача 5
`
Дроби.
Создайте сущность Дробь со следующими особенностями:
 Имеет числитель: целое число
 Имеет знаменатель: целое число
 Дробь может быть создана с указанием числителя и знаменателя
 Может вернуть строковое представление вида “числитель/знаменатель”
 Может выполнять операции сложения, вычитания, умножения и деления с другой Дробью
или целым числом. Результатом операции должна быть новая Дробь (таким образом,обе
исходные дроби не изменяются)
Затем необходимо выполнить следующие задачи:
1. Создать несколько экземпляров дробей.
2. Написать по одному примеру использования каждого метода.
3. Вывести на экран примеры и результаты их выполнения в формате «1/3 * 2/3 = 2/9»
4. Посчитать f1.sum(f2).div(f3).minus(5)
`

##### Пример 1:
Дробь 1: 1/3
Дробь 2: 2/3
Число: 5

Результат:
1/3 * 2/3 - 5 = -43/9

#### Описание решения
Инициализировал класс `Fraction` с полями `numerator` и `denominator`. Перегрузил операторы `+` `-` `*` `/`.
Сложения дробей:
Числитель получается так:
(числитель первой дроби * знаминатель второй дроби) + (числитель второй дроби * знаминатель второй дроби).

Знаметаль получается так:
(знаминатель первой дроби * знамиеатель второй дроби)

потом сокращаем числитель и знаминатель если можем

Вычитание дробей:
Числитель получается так:
(числитель первой дроби * знаминатель второй дроби) - (числитель второй дроби * знаминатель второй дроби).

Знаметаль получается так:
(знаминатель первой дроби * знаминатель второй дроби)

потом сокращаем числитель и знаминатель если можем

Сложения дроби и числа:
Числитель получается так:
(числитель дроби ) + (число * знаминатель дроби).

потом сокращаем числитель и знаминатель если можем

Вычитание дроби и числа:
Числитель получается так:
(числитель дроби ) - (число * знаминатель дроби).

потом сокращаем числитель и знаминатель если можем

Умножение дробей:
Числитель получается так:
(числитель первой дроби * числитель второй дроби)
Знаметаль получается так:
(знаминатель первой дроби * знаминатель второй дроби)

потом сокращаем числитель и знаминатель если можем

Умножение дроби на число:
Числитель получается так:
(числитель первой дроби * число)

потом сокращаем числитель и знаминатель если можем

Деление дробей:
Числитель получается так:
(числитель первой дроби * знаминатель второй дроби)
Знаметаль получается так:
(знаминатель первой дроби * числитель второй дроби)

потом сокращаем числитель и знаминатель если можем

Деление дроби на чилсо:
Знаминатель получается так:
знаминтаель дроби * число

потом сокращаем числитель и знаминатель если можем
